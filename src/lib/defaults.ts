import { templateConfig as model1 } from "./templates/model-1";

/* ============================================
   CONFIG PADRÃO
   Usa o modelo 1 (Premium Dourado) como base
   para novas propostas
   ============================================ */

export const defaultProposalConfig = model1;
